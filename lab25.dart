// Common Collection Functions
// Collection = set of data and it can be 0 to many elements
// in one variable

void main() {
  var x = []; // Empty Array
  var y = ["Ant", "Bat", "Cat"];
  
  print(x.isEmpty); // true if there are no elements
  print(x.isNotEmpty); // true if there are no elements
  
  print(x.isEmpty); // true if there are no elements
  print(x.isNotEmpty); // true if there are no elements
}
